//1
function plus() {
    let inputElement = document.querySelector('#numberInput');
    inputElement.value = Number(inputElement.value)+1;
}
function minus() {
    let inputElement = document.querySelector('#numberInput');
    inputElement.value = Number(inputElement.value)-1;
}